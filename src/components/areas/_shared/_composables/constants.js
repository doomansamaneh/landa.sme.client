import { code, title, isActive, actions } from "src/constants/columns";
export const baseInfoColumns = [code, title, isActive, actions];
export const reportColumns = [code, title, isActive, actions];
